<% provide(:title, "Update Car") %>
<% provide(:path, @view_renderer.lookup_context.find_template(@virtual_path).identifier.to_s) %>
<% provide(:submit_label, "Update Car") %>

<section class="container-fluid banner">
  <div class="container">
    <h1>Update Car</h1>
  </div>
</section>
<section class="container align-slef-center m-top">
  <div class="row">
    <div class="col"></div>
    <div class="col">
      <%= render 'form', car: @car %>
    </div>
    <div class="col"></div>
  </div>
</section>